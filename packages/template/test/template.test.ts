/*
 * Copyright The OpenTelemetry Authors
 * SPDX-License-Identifier: Apache-2.0
 */

import * as assert from 'assert';

describe('Template', () => {
  it('should pass', () => {
    assert.ok(true);
  });
});
