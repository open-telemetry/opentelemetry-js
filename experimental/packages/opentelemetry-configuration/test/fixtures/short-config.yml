file_format: "1.0-rc.1"
disabled: false
resource:
  attributes_list: service.instance.id=123

tracer_provider:
  processors:
    - batch:
        exporter:
          otlp_http:
            headers_list: api-key=1234
