{%- import 'docstring.ts.j2' as d %}
/*
 * Copyright The OpenTelemetry Authors
 * SPDX-License-Identifier: Apache-2.0
 */

//---------------------------------------------------------------------------------------------------------------------------
// DO NOT EDIT, this is an Auto-generated file from scripts/semconv-genai/templates/registry/ts-experimental/attributes.ts.j2
//---------------------------------------------------------------------------------------------------------------------------

{% for attribute in ctx.attributes | attribute_sort %}
{% if attribute.key not in params.excluded_attributes %}
{% if attribute is not stable %}
{{d.docstring(attribute, "attribute")}}
{% if attribute.type is not template_type %}
export const ATTR_{{ attribute.key | screaming_snake_case }} = '{{attribute.key}}' as const;

{% else %}
export const ATTR_{{ attribute.key | screaming_snake_case }} = (key: string) => `{{attribute.key}}.${key}`;

{% endif %}
{% endif %}
{% if attribute.type is mapping %}
{% for member in attribute.type.members | sort(attribute='value') %}
{% if member is not stable %}
{{d.docstring_enum_value(member, attribute)}}
export const {{ attribute.key | screaming_snake_case }}_VALUE_{{ member.id | screaming_snake_case }} = {{ member.value | print_member_value }} as const;

{% endif %}
{% endfor %}
{% endif %}
{% endif %}
{% endfor %}
{% if (ctx.attributes | length) == 0 %}
// There are no experimental GenAI attributes. This keeps the file a valid ES
// module, so that `export * from './experimental_attributes'` compiles.
export {};
{% endif %}
